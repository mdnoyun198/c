#include <stdio.h>

int main()
{

    int a = 1;
    float b = 1.5;
    char c = 'a';

    printf("this is number %d\n", a);

    printf(" is this floiting number%f\n", b);

    printf(" its is charektar %c\n", c);

    printf("hello // im rokey \n \' \" \\n ");

    return 0;
}

/*
  An 'int' variable is used to store whole numbers without any decimal points.

  A 'float' variable is used when working with numbers that contain a decimal point.

  A 'char' variable can store only a single character enclosed in single quotes.
*/
